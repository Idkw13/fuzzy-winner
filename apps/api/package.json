{
  "name": "@fuzzy-winner/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "go build -o api main.go",
    "dev": "go run main.go",
    "start": "./api",
    "test": "go test ./...",
    "lint": "golangci-lint run",
    "clean": "rm -f api"
  },
  "dependencies": {},
  "devDependencies": {}
}
